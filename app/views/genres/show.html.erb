<p style="color: green"><%= notice %></p>

<h1 class="title is-2"><%=@genre.name%></h1>
<p class="title is-5 has-text-centered">Games in this Genre:</p>
<div class="columns">    
      <%@genre.Games.each do |game|%>
        <div class="column">
            <%= render partial: "games/game", object: game %>    
        </div>
      <%end%>
</div>
