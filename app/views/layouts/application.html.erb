<!DOCTYPE html>
<html>
  <head>
    <title>GameShack</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar is-dark" aria-label="main navigation">
      <div class="navbar-menu">
        <div class="navbar-start">
          <a class="navbar-item" href=<%=home_path%>>
            Home
          </a>
          <a class="navbar-item" href=<%=games_path%>>
            Games
          </a>
          <a class="navbar-item" href=<%=platforms_path%>>
            Platforms
          </a>    
          <a class="navbar-item" href=<%=genres_path%>>
            Genres
          </a>     
          <div class="navbar-item"> 
            <%= form_with(url: '/search', method: 'get', local: true) do %>
              <%= text_field_tag(:search) %>
              <select name="platforms" id="platforms">
                  <option value="All">All Platforms</option>
                <% Platform.all.each do |p|%>
                  <option value="<%=p.id%>"><%=p.name%></option>
                <%end%>
              </select>
              <%= submit_tag("Search") %>
            <% end %>
          </div>
        </div>
        <div class="navbar-end">          
          <div class="navbar-item">
            <%=button_to "Admin", admin_root_path, class: "button is-danger"%>&nbsp
            <%if user_signed_in?%>
              <%=button_to "Account", edit_user_registration_path, class: "button is-primary"%>&nbsp
              <%=button_to "Sign Out", destroy_user_session_path, class: "button is-danger"%>
            <%else%>
              <%=button_to "Sign Up", new_user_registration_path, class: "button is-primary"%>&nbsp
              <%=button_to "Sign In", new_user_session_path, class: "button is-primary"%>
            <%end%>
            
          </div>
        </div>
      </div>
    </nav>
    <p class = "notice"><%=notice%></p>
    <p class = "alert"><%=alert%></p>
    <details>
      <summary>Cart</summary>
      <%if @cart.empty?%>
        <p>Cart is empty.</p>
      <%else%>
        <ul>
          <%@cart.each do |product|%>
            <li><%=product.name%></li>
            <%if(product.is_a? Game)%>
              <%=button_to "Remove",remove_game_from_cart_path(product.id), method: :delete, class: "button is-small is-danger"%>
            <%else%>
              <%=button_to "Remove",remove_platform_from_cart_path(product.id), method: :delete, class: "button is-small is-danger"%>
            <%end%>
          <%end%>
        </ul>
      <%end%>
    </details>
    <%= yield %>
  </body>
</html>
